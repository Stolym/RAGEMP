using System;
using System.Collections.Generic;
using System.Text;

namespace main_client.Utils
{
    /*class GenerateIntro
    {
        public static string intro_id_card() {
            string tmp = "";
            Random rand = new Random();

            if (rand == null)
                return "BUg!!!..";

            for (int i = 0; i < 10; i++)
                tmp += rand.Next(0, 1) != 1 ? (char)rand.Next(48, 57) : (char)rand.Next(65, 90);
            return tmp; 
        }
    }*/
}
